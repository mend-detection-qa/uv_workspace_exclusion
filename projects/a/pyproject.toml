[project]
name = "project-a"
version = "1.0.0"
description = "Project A - included in workspace"
requires-python = ">=3.10"

dependencies = [
    "fastapi>=0.109.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
